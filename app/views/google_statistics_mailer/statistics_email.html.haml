!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
  %body

    %h3 Search statistics
    %p
      - @unique_queries.each do |unique_query|
        %p Query: "#{unique_query}"
        %p Search quantity: #{@google_queries.where(value: unique_query).count}
        %table.table.table-striped
          %tr
            %th Value
            %th Date
          -@google_queries.where(value: unique_query).each do |query|
            %tr
              %td= query.value
              %td= query.created_at.strftime("%Y-%m-%d %H:%M:%S")
