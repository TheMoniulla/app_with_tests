%h1 Expenses categories
= link_to 'New expenses category', new_expenses_category_path, class: 'btn btn-success'

%table.table.table-striped
  %tr
    %th Name
    %th Description
    %th Actions
  - expenses_categories.each do |category|
    %tr
      %td= category.name
      %td= category.description
      %td
        = link_to 'Edit', edit_expenses_category_path(expenses_category.id)
        = link_to 'Delete', expenses_category_path(expenses_category.id), method: :delete, data: {confirm: 'Are you sure?'}
